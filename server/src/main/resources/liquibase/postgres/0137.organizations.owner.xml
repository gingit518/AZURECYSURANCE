<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
	xmlns="http://www.liquibase.org/xml/ns/dbchangelog/1.7"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog/1.7
         http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-1.7.xsd">

	<changeSet id="0137_organizations_owner" author="Eugene A. Kalosha">

		<sql>
			ALTER TABLE organizations ADD COLUMN owner_id integer DEFAULT NULL;
			ALTER TABLE organizations ADD CONSTRAINT fk_organizations_to_owner_id FOREIGN KEY (owner_id) REFERENCES users (id) MATCH SIMPLE ON UPDATE NO ACTION ON DELETE NO ACTION;
			CREATE INDEX i_organization_owner ON organizations (owner_id);

		</sql>

		<rollback>
			<sql>
				ALTER TABLE DROP COLUMN owner_id;
			</sql>
		</rollback>
	</changeSet>

</databaseChangeLog>
