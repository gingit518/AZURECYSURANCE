<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
	xmlns="http://www.liquibase.org/xml/ns/dbchangelog/1.7"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog/1.7
         http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-1.7.xsd">

	<changeSet id="0232.metric_domains_new_values" author="Daniel A. Kolesnik">

		<sql>

			INSERT INTO metric_domains (id, name, description, code) VALUES
				(6, 'Confidentiality', '', 'confidentiality')
				, (7, 'Integrity', '', 'integrity')
			;

			ALTER SEQUENCE metric_domains_id_seq RESTART WITH 8;


			INSERT INTO variable_types (id, name, relation) VALUES
				(11, 'CONFIDENTIALITY', 'RISK_METRIC')
				, (12, 'INTEGRITY', 'RISK_METRIC')
			;

			ALTER SEQUENCE variable_types_id_seq RESTART WITH 13;

		</sql>

		<rollback>
			<sql>
				DELETE FROM metric_domains md WHERE md.id IN (6, 7);
				DELETE FROM variable_types md WHERE md.id IN (11, 12);
			</sql>
		</rollback>
	</changeSet>

</databaseChangeLog>
