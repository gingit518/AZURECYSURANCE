<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
	xmlns="http://www.liquibase.org/xml/ns/dbchangelog/1.7"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog/1.7
         http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-1.7.xsd">

	<changeSet id="00437.organization_update" author="Eugene A. Kalosha">

		<sql>
			ALTER TABLE organizations ADD COLUMN IF NOT EXISTS uid varchar(64) DEFAULT null;
			ALTER TABLE organizations ADD COLUMN IF NOT EXISTS platform_type varchar(64) DEFAULT null;
			ALTER TABLE organizations ADD COLUMN IF NOT EXISTS asset_type varchar(64) DEFAULT null;
			ALTER TABLE organizations ADD COLUMN IF NOT EXISTS amount_of_data_in_terabytes float DEFAULT null;
			ALTER TABLE organizations ADD COLUMN IF NOT EXISTS replication_factor float DEFAULT null;
			CREATE INDEX i_organization_uid ON organizations (uid);
		</sql>

		<rollback>
			<sql>
				DROP INDEX CONCURRENTLY IF EXISTS i_organization_uid;
				ALTER TABLE organizations DROP COLUMN replication_factor;
				ALTER TABLE organizations DROP COLUMN amount_of_data_in_terabytes;
				ALTER TABLE organizations DROP COLUMN asset_type;
				ALTER TABLE organizations DROP COLUMN platform_type;
				ALTER TABLE organizations DROP COLUMN uid;
			</sql>
		</rollback>
	</changeSet>

</databaseChangeLog>
