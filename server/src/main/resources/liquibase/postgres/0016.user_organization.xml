<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
	xmlns="http://www.liquibase.org/xml/ns/dbchangelog/1.7"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog/1.7
         http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-1.7.xsd">

	<changeSet id="0016-user-organization" author="Eugene A. Kalosha">

		<sql>
			ALTER TABLE users ADD COLUMN organization_id integer DEFAULT NULL;
			ALTER TABLE users ADD CONSTRAINT fk_users_to_organizations FOREIGN KEY (organization_id) REFERENCES organizations (id) MATCH SIMPLE ON UPDATE NO ACTION ON DELETE NO ACTION;

			CREATE INDEX i_user_organization ON users (organization_id);
		</sql>

		<rollback>
			<sql>
				ALTER TABLE DROP COLUMN organization_id;
			</sql>
		</rollback>
	</changeSet>

</databaseChangeLog>
